---
title: "Figures"
author: "Andrew Chen"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output:
  html_document:
    theme: 'united'
    toc: true
    toc_depth: 1
    toc_float: true
    df_print: paged
    code_folding: hide
---

```{r, echo=FALSE, message=FALSE}
library(tidyverse)
library(sigrecon)
library(ggplot2)
library(ggbeeswarm)
library(arrow)
library(ComplexHeatmap)
library(circlize)
library(grid)

PROJECT_PATH <- file.path(Sys.getenv("MLAB"), "projects/brcameta/projects/sig_recon")
RESULTS_PATH <- file.path(PROJECT_PATH, "results/eval")
```

```{r}
summarize_method <- function(df, method_name){

  per_source <- df %>%
    group_by(source) %>%
    summarise(
      jacc_p = wilcox.test(jacc_TRUE, jacc_FALSE,
                           paired = TRUE,
                           alternative = "greater")$p.value,

      nes_p = wilcox.test(NES_TRUE, NES_FALSE,
                          paired = TRUE,
                          alternative = "greater")$p.value,

      jacc_prop = mean(jacc_TRUE > jacc_FALSE),
      nes_prop = mean(NES_TRUE > NES_FALSE),
      .groups="drop"
    )

  tibble(
    method = method_name,
    metric = c("Jaccard","NES"),
    fisher_p = c(
      fishers_meta_p(per_source$jacc_p),
      fishers_meta_p(per_source$nes_p)
    ),
    prop = c(
      mean(per_source$jacc_prop),
      mean(per_source$nes_prop)
    )
  )
}
```

# Tahoe {.tabset}

## MOA Delta NES Across Methods
```{r}
tahoe_metadata_path <- file.path(
  Sys.getenv("AGED"),
  "CBMrepositoryData/perturbational_data/tahoe/metadata/drug_metadata.parquet"
)

normalize_drug_name <- function(x) {
  x %>%
    str_to_lower() %>%
    str_replace_all("[^[:alnum:]]+", "")
}

normalize_stack_target <- function(source, target) {
  if_else(
    target == "target_context" & str_detect(source, "^nci_h23_"),
    str_remove(source, "^nci_h23_"),
    target
  )
}

clean_moa_fine_label <- function(x) {
  x %>%
    str_remove(regex("\\s+(agonist|activator|inhibitor|antagonist)$", ignore_case = TRUE)) %>%
    str_squish()
}

clean_moa_broad_label <- function(x) {
  recode(
    x,
    "activator/agonist" = "Activator/\nAgonist",
    "inhibitor/antagonist" = "Inhibitor/\nAntagonist"
  )
}

tahoe_drug_metadata <- arrow::read_parquet(tahoe_metadata_path) %>%
  as_tibble() %>%
  select(drug, `moa-broad`, `moa-fine`) %>%
  distinct() %>%
  mutate(drug_key = normalize_drug_name(drug)) %>%
  distinct(drug_key, .keep_all = TRUE)

tahoe_eval_specs <- tribble(
  ~method, ~regime, ~file,
  "Mean", "ctrl", "ctrl_mean_eval_table.rds",
  "Mean", "10th", "ctrl_10th_mean_eval_table.rds",
  "Mean", "90th", "ctrl_90th_mean_eval_table.rds",
  "NetworkProp", "ctrl", "ctrl_networkprop_eval_table.rds",
  "NetworkProp", "10th", "network_prop_10th_eval_table.rds",
  "NetworkProp", "90th", "network_prop_90th_eval_table.rds",
  "ProjectCor (Eigen)", "ctrl", "ctrl_projectcor_eigen_eval_table.rds",
  "ProjectCor (Eigen)", "10th", "10th_projectcor_eigen_eval_table.rds",
  "ProjectCor (Eigen)", "90th", "90th_projectcor_eigen_eval_table.rds",
  "ProjectCor (GSVA)", "ctrl", "ctrl_projectcor_gsva_eval_table.rds",
  "ProjectCor (GSVA)", "10th", "10th_projectcor_gsva_eval_table.rds",
  "ProjectCor (GSVA)", "90th", "90th_projectcor_gsva_eval_table.rds",
  "Orthos", "ctrl", "ctrl_orthos_residual_eval_table.rds",
  "Stack", "ctrl", "stack_ctrl_eval_table.rds",
  "Stack", "10th", "stack_10th_eval_table.rds",
  "Stack", "90th", "stack_90th_eval_table.rds"
) %>%
  mutate(path = file.path(RESULTS_PATH, "tahoe", file)) %>%
  filter(file.exists(path))

read_tahoe_eval <- function(path, method, regime) {
  print(path)
  readRDS(path) %>%
    as_tibble() %>%
    transmute(
      method = method,
      regime = regime,
      gene,
      target = normalize_stack_target(source, target),
      delta_NES = NES_TRUE - NES_FALSE
    ) %>%
    group_by(method, regime, gene, target) %>%
    summarise(delta_NES = median(delta_NES, na.rm = TRUE), .groups = "drop")
}

tahoe_moa_delta_joined_df <- pmap_dfr(
  tahoe_eval_specs %>% select(path, method, regime),
  read_tahoe_eval
) %>%
  mutate(regime = if_else(regime == "ctrl", "Control", regime)) %>% 
  mutate(
    method = factor(
      method,
      levels = c("Mean", "NetworkProp", "ProjectCor (Eigen)",
                 "ProjectCor (GSVA)", "Orthos", "Stack")
    ),
    regime = factor(regime, levels = c("90th", "10th", "Control")),
    drug_key = normalize_drug_name(gene)
  ) %>%
  left_join(tahoe_drug_metadata, by = "drug_key")

tahoe_unmatched_moa_drugs <- tahoe_moa_delta_joined_df %>%
  filter(is.na(`moa-fine`)) %>%
  distinct(gene) %>%
  arrange(gene)

saveRDS(
  tahoe_unmatched_moa_drugs,
  file.path(RESULTS_PATH, "tahoe/tahoe_moa_delta_unmatched_drugs.rds")
)

tahoe_moa_delta_df <- tahoe_moa_delta_joined_df %>%
  filter(
    !is.na(`moa-fine`),
    `moa-fine` != "unclear",
    `moa-broad` %in% c("activator/agonist", "inhibitor/antagonist")
  ) %>%
  mutate(
    moa_broad_group = factor(
      clean_moa_broad_label(`moa-broad`),
      levels = c("Inhibitor/\nAntagonist", "Activator/\nAgonist")
    ),
    moa_fine_label = clean_moa_fine_label(`moa-fine`)
  )

saveRDS(
  tahoe_moa_delta_df,
  file.path(RESULTS_PATH, "tahoe/tahoe_moa_delta_nes_plot_df.rds")
)
```

```{r, fig.width=14, fig.height=10}
moa_order <- tahoe_moa_delta_df %>%
  filter(regime == "90th") %>%
  group_by(moa_broad_group, moa_fine_label) %>%
  summarise(median_delta_NES = median(delta_NES, na.rm = TRUE), .groups = "drop") %>%
  group_by(moa_broad_group) %>%
  arrange(desc(median_delta_NES)) %>%
  summarise(moa_fine_label = list(moa_fine_label), .groups = "drop")

moa_levels <- c(
  rev(moa_order$moa_fine_label[[which(moa_order$moa_broad_group == "Activator/\nAgonist")]]),
  rev(moa_order$moa_fine_label[[which(moa_order$moa_broad_group == "Inhibitor/\nAntagonist")]])
)

ggplot(
  tahoe_moa_delta_df %>%
    mutate(moa_fine_label = factor(moa_fine_label, levels = moa_levels)),
  aes(x = delta_NES, y = moa_fine_label)
) +
  geom_vline(xintercept = 0, color = "grey70", linewidth = 0.35) +
  geom_boxplot(
    aes(fill = regime),
    outlier.shape = NA,
    alpha = 0.65,
    width = 0.55
  ) +
  ggbeeswarm::geom_quasirandom(
    aes(color = method, group = moa_fine_label),
    width = 0.18,
    size = 0.6,
    alpha = 0.25,
    groupOnX = FALSE
  ) +
  facet_grid(
    rows = vars(moa_broad_group),
    cols = vars(regime),
    scales = "free_y",
    space = "free_y",
    switch = "y"
  ) +
  scale_fill_brewer(palette = "Pastel1", guide = "none") +
  scale_y_discrete(labels = function(x) str_wrap(x, width = 28), position = "right") +
  labs(
    x = "Delta NES",
    y = NULL,
    color = "Method"
  ) +
  theme_bw(base_size = 18) +
  theme(
    panel.grid.minor = element_blank(),
    axis.title.y = element_blank(),
    strip.placement = "outside",
    strip.background.y = element_blank(),
    strip.text.y.left = element_text(size = 18, face = "bold", angle = 0),
    legend.position = "right",
    legend.text = element_text(size = 15),  
    legend.title = element_text(size = 18),
    axis.text = element_text(size = 18),
    strip.text = element_text(size = 18)
  )
```

```{r}
ggsave(
  file.path(PROJECT_PATH, "results/plots/tahoe_moa_fine_delta_NES_methods.png"),
  width = 14,
  height = 10,
  dpi = 400
)
```

## MOA Gain Heatmaps
```{r}
tahoe_moa_gain_df <- tahoe_moa_delta_df %>%
  select(method, regime, gene, target, moa_broad_group, moa_fine_label, delta_NES) %>%
  pivot_wider(names_from = regime, values_from = delta_NES) %>%
  mutate(
    gain_10th = `10th` - Control,
    gain_90th = `90th` - Control
  ) %>%
  pivot_longer(
    c(gain_10th, gain_90th),
    names_to = "comparison",
    values_to = "gain"
  ) %>%
  filter(is.finite(gain)) %>%
  group_by(comparison, moa_broad_group, moa_fine_label, method) %>%
  summarise(median_gain = median(gain, na.rm = TRUE), .groups = "drop")

saveRDS(
  tahoe_moa_gain_df,
  file.path(RESULTS_PATH, "tahoe/tahoe_moa_gain_heatmap_df.rds")
)

plot_gain_heatmap <- function(gain_df, comparison_name, title) {
  gain_wide <- gain_df %>%
    filter(comparison == comparison_name) %>%
    select(moa_broad_group, moa_fine_label, method, median_gain) %>%
    pivot_wider(names_from = method, values_from = median_gain) %>%
    arrange(moa_broad_group, moa_fine_label)
  
  row_group <- gain_wide$moa_broad_group
  row_group <- factor(row_group, levels = c("Inhibitor/\nAntagonist", "Activator/\nAgonist"))
  gain_mat <- gain_wide %>%
    select(-moa_broad_group) %>%
    column_to_rownames("moa_fine_label") %>%
    as.matrix()
  
  max_abs <- max(abs(gain_mat), na.rm = TRUE)
  
  Heatmap(
    gain_mat,
    name = "Median gain",
    col = circlize::colorRamp2(c(-max_abs, 0, max_abs), c("#3b4cc0", "white", "#b40426")),
    na_col = "grey90",
    cluster_rows = TRUE,
    show_row_dend = FALSE,
    show_column_dend = FALSE,
    cluster_columns = TRUE,
    row_split = row_group,
    row_gap = unit(5, "mm"),
    cluster_row_slices = FALSE,
    row_names_side = "right",
    row_names_gp = gpar(fontsize = 17), 
    column_names_rot = 45,
    column_names_gp = gpar(fontsize = 17), 
    column_title = title,
    column_title_gp = gpar(fontsize = 17, fontface = "bold"), 
    row_title_side = "left",
    row_title_rot = 0,
    row_title_gp = gpar(fontsize = 17, fontface = "bold"),
    cell_fun = function(j, i, x, y, width, height, fill) {
      value <- gain_mat[i, j]
      if (is.finite(value)) {
        grid::grid.text(sprintf("%.2f", value), x, y, gp = grid::gpar(fontsize = 14))
      }
    },
    heatmap_legend_param = list(title = "Median\nDelta NES",
                                title_gp = gpar(fontsize = 16, fontface = "bold"),
                                labels_gp = gpar(fontsize = 15, col = "black"))
  )
}

ht_10th <- plot_gain_heatmap(tahoe_moa_gain_df, "gain_10th", "10th - ctrl")
ht_90th <- plot_gain_heatmap(tahoe_moa_gain_df, "gain_90th", "90th - ctrl")
```

```{r, fig.width=11, fig.height=10}
draw(ht_10th)
```

```{r}
png(file.path(PROJECT_PATH, "results/plots/tahoe_moa_gain_10th_minus_ctrl_heatmap.png"),
    width = 11, height = 10, units = "in", res = 400)
draw(ht_10th)
dev.off()
```

```{r, fig.width=11, fig.height=10}
draw(ht_90th)
```

```{r}
png(file.path(PROJECT_PATH, "results/plots/tahoe_moa_gain_90th_minus_ctrl_heatmap.png"),
    width = 11, height = 10, units = "in", res = 400)
draw(ht_90th)
dev.off()
```

## MOA TI Quadrant Scatter
```{r}
tahoe_trade_metrics <- readRDS(file.path(PROJECT_PATH,"results/plots/trade_metric_correlations/tahoe_projectCor/context_trade_metrics.rds"))

tahoe_source_ti <- tahoe_trade_metrics %>%
  filter(context_key == normalize_drug_name("NCI-H23")) %>%
  mutate(drug_key = normalize_drug_name(drug)) %>%
  select(drug_key, source_TI = TI) %>%
  distinct(drug_key, .keep_all = TRUE)

tahoe_target_ti <- tahoe_trade_metrics %>%
  mutate(drug_key = normalize_drug_name(drug)) %>%
  select(target_key = context_key, drug_key, target_TI = TI) %>%
  distinct(target_key, drug_key, .keep_all = TRUE)

tahoe_moa_ti_df <- tahoe_moa_delta_df %>%
  mutate(target_key = normalize_drug_name(target)) %>%
  left_join(tahoe_source_ti, by = "drug_key") %>%
  left_join(tahoe_target_ti, by = c("target_key", "drug_key")) %>%
  mutate(delta_TI = target_TI - source_TI)

tahoe_moa_ti_summary_df <- tahoe_moa_ti_df %>%
  filter(
    is.finite(delta_NES),
    is.finite(source_TI),
    is.finite(target_TI),
    is.finite(delta_TI)
  ) %>%
  group_by(regime, moa_broad_group, `moa-fine`, moa_fine_label) %>%
  summarise(
    median_delta_NES = median(delta_NES, na.rm = TRUE),
    median_source_TI = median(source_TI, na.rm = TRUE),
    median_target_TI = median(target_TI, na.rm = TRUE),
    median_delta_TI = median(delta_TI, na.rm = TRUE),
    n_drugs = n_distinct(gene),
    .groups = "drop"
  )

saveRDS(
  tahoe_moa_ti_summary_df,
  file.path(RESULTS_PATH, "tahoe/tahoe_moa_ti_quadrant_summary_df.rds")
)

plot_moa_ti_quadrant <- function(df, x_col, x_lab, title) {
  moa_legend_order <- df %>%
    filter(regime == "90th") %>%
    arrange(desc(median_delta_NES)) %>%
    pull(`moa-fine`)
  moa_legend_order <- c(moa_legend_order, setdiff(unique(df$`moa-fine`), moa_legend_order))
  
  plot_df <- df %>%
    mutate(`moa-fine` = factor(`moa-fine`, levels = moa_legend_order))
  
  size_breaks <- function(lims) {
    candidates <- c(1, 2, 3, 5, 7, 10, 15, 20, 30, 50)
    candidates[candidates >= ceiling(lims[1]) & candidates <= floor(lims[2])]
  }
  fit_stats <- df %>%
    filter(is.finite(.data[[x_col]]), is.finite(median_delta_NES)) %>%
    group_by(regime) %>%
    group_modify(function(dat, key) {
      dat <- dat %>% mutate(.x_value = .data[[x_col]])
      if (nrow(dat) < 3 || n_distinct(dat$.x_value) < 2 || n_distinct(dat$median_delta_NES) < 2) {
        return(tibble(label = "R2 = NA\np = NA"))
      }
      fit <- lm(median_delta_NES ~ .x_value, data = dat)
      fit_summary <- summary(fit)
      tibble(
        label = sprintf(
          "R2 = %.2f\np = %.2g",
          fit_summary$r.squared,
          coef(fit_summary)[".x_value", "Pr(>|t|)"]
        )
      )
    }) %>%
    ungroup()
  
  ggplot(plot_df, aes(x = .data[[x_col]], y = median_delta_NES)) +
    geom_vline(xintercept = 0, linetype = "dashed", color = "grey55", linewidth = 0.45) +
    geom_hline(yintercept = 0, linetype = "dashed", color = "grey55", linewidth = 0.45) +
    geom_hline(yintercept = 0, color = "grey80", linewidth = 0.35) +
    geom_smooth(method = "lm", se = FALSE, color = "black", linewidth = 0.55) +
    geom_point(aes(size = n_drugs, fill = `moa-fine`), shape = 21, color = "grey20", alpha = 0.82) +
    geom_text(
      data = fit_stats,
      aes(x = Inf, y = Inf, label = label),
      inherit.aes = FALSE,
      hjust = 1.08,
      vjust = 1.2,
      size = 4.2
    ) +
    facet_grid(. ~ regime) +
    scale_size(range = c(2, 8), breaks = size_breaks) +
    labs(
      title = title,
      x = x_lab,
      y = "Median delta NES",
      fill = "MOA",
      size = "N drugs"
    ) +
    theme_bw(base_size = 16) +
    theme(
      panel.grid.minor = element_blank(),
      legend.position = "right",
      strip.text = element_text(size = 16)
    )
}

source_ti_quadrant <- plot_moa_ti_quadrant(
  tahoe_moa_ti_summary_df,
  "median_source_TI",
  "Median source TI",
  "MOA-level source TI vs delta NES"
)

target_ti_quadrant <- plot_moa_ti_quadrant(
  tahoe_moa_ti_summary_df,
  "median_target_TI",
  "Median target TI",
  "MOA-level target TI vs delta NES"
)

delta_ti_quadrant <- plot_moa_ti_quadrant(
  tahoe_moa_ti_summary_df,
  "median_delta_TI",
  "Median delta TI",
  "MOA-level delta TI vs delta NES"
)
```

```{r, fig.width=14, fig.height=6}
source_ti_quadrant
```

```{r}
ggsave(
  file.path(PROJECT_PATH, "results/plots/tahoe_moa_source_TI_delta_NES_quadrant.png"),
  source_ti_quadrant,
  width = 14,
  height = 6,
  dpi = 400
)
```

```{r, fig.width=14, fig.height=6}
target_ti_quadrant
```

```{r}
ggsave(
  file.path(PROJECT_PATH, "results/plots/tahoe_moa_target_TI_delta_NES_quadrant.png"),
  target_ti_quadrant,
  width = 14,
  height = 6,
  dpi = 400
)
```

```{r, fig.width=14, fig.height=6}
delta_ti_quadrant
```

```{r}
ggsave(
  file.path(PROJECT_PATH, "results/plots/tahoe_moa_delta_TI_delta_NES_quadrant.png"),
  delta_ti_quadrant,
  width = 14,
  height = 6,
  dpi = 400
)
```

## Control {.tabset}
```{r}
tahoe_no_change_df <- readRDS(file.path(RESULTS_PATH, "tahoe/no_change_eval_table.rds"))
tahoe_ctrl_mean_df <- readRDS(file.path(RESULTS_PATH, "tahoe/ctrl_mean_eval_table.rds"))
tahoe_ctrl_net_df <- readRDS(file.path(RESULTS_PATH, "tahoe/ctrl_networkprop_eval_table.rds"))
tahoe_ctrl_proj_e_df <- readRDS(file.path(RESULTS_PATH, "tahoe/ctrl_projectcor_eigen_eval_table.rds"))
tahoe_ctrl_proj_g_df <- readRDS(file.path(RESULTS_PATH, "tahoe/ctrl_projectcor_gsva_eval_table.rds"))
tahoe_orthos_df <- readRDS(file.path(RESULTS_PATH, "tahoe/ctrl_orthos_residual_eval_table.rds"))
tahoe_stack_df <- readRDS(file.path(RESULTS_PATH, "tahoe/stack_ctrl_eval_table.rds"))
```

```{r}
plot_df <- bind_rows(
  tahoe_no_change_df %>%
    transmute(drug = gene, method = "No change", jacc, NES),

  tahoe_ctrl_mean_df %>%
    transmute(drug = gene, method = "Mean", jacc = jacc_TRUE, NES = NES_TRUE),

  tahoe_ctrl_net_df %>%
    transmute(drug = gene, method = "NetworkProp", jacc = jacc_TRUE, NES = NES_TRUE),

  tahoe_ctrl_proj_e_df %>%
    transmute(drug = gene, method = "ProjectCor (Eigen)", jacc = jacc_TRUE, NES = NES_TRUE),

  tahoe_ctrl_proj_g_df %>%
    transmute(drug = gene, method = "ProjectCor (GSVA)", jacc = jacc_TRUE, NES = NES_TRUE),
  
  tahoe_orthos_df %>%
    transmute(drug = gene, method = "Orthos", jacc = jacc_TRUE, NES = NES_TRUE),
  
  tahoe_stack_df %>%
    transmute(drug = gene, method = "Stack", jacc = jacc_TRUE, NES = NES_TRUE),
)

plot_long <- plot_df %>%
  dplyr::rename(Jaccard = jacc) %>%
  pivot_longer(c(Jaccard, NES), names_to = "metric", values_to = "value")

saveRDS(plot_long, file.path(RESULTS_PATH, "tahoe/ctrl_unpaired_plot_df.rds"))

summary_df <- bind_rows(
  summarize_method(tahoe_ctrl_mean_df, "Mean"),
  summarize_method(tahoe_ctrl_net_df, "NetworkProp"),
  summarize_method(tahoe_ctrl_proj_e_df, "ProjectCor (Eigen)"),
  summarize_method(tahoe_ctrl_proj_g_df, "ProjectCor (GSVA)"),
  summarize_method(tahoe_orthos_df, "Orthos"),
  summarize_method(tahoe_stack_df, "Stack")
)
saveRDS(summary_df, file.path(RESULTS_PATH, "tahoe/ctrl_paired_plot_df.rds"))
```

### Unpaired
```{r}
baseline_means <- plot_long %>%
  filter(method == "No change") %>%
  group_by(metric) %>%
  summarise(yintercept = median(value), .groups = "drop")

ggplot(plot_long, aes(method, value, fill = method)) +
  geom_violin(
    width = 0.6,
    alpha = 0.3,
    color = NA,
    trim = TRUE
  ) +
  ggbeeswarm::geom_quasirandom(
    width = 0.15,
    alpha = 0.15,
    size = 0.05,
    groupOnX = TRUE
  ) +
  stat_summary(
    fun = median,
    geom = "point",
    shape = 21,
    size = 2.5,
    fill = "white"
  ) +
  geom_hline(
    data = baseline_means,
    aes(yintercept = yintercept),
    inherit.aes = FALSE,
    linetype = "dotted",
    color = "grey50",
    linewidth = 0.8
  ) +
  facet_wrap(~metric, scales = "free_y") +
  scale_fill_brewer(palette = "Pastel1") +
  scale_x_discrete(
    limits = c("No change", "Mean", "NetworkProp", "ProjectCor (Eigen)",
               "ProjectCor (GSVA)", "Orthos", "Stack")
  ) +
  labs(x = "", y = "Score") +
  theme_classic(base_size = 15) +
  theme(
    axis.text.x = element_text(angle = 35, hjust = 1),
    legend.position = "none",
    strip.text = element_text(face = "bold"),
    plot.title = element_text(face = "bold")
  )
```
```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/tahoe_ctrl_unpaired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

### Paired
```{r}
ggplot(summary_df,
       aes(x = -log10(fisher_p),
           y = method,
           size = prop,
           color = -log10(fisher_p))) +

  geom_point(alpha = 0.9) +

  facet_wrap(~metric) +
  coord_cartesian(xlim = c(-0.02, 0.125)) +
  scale_color_viridis_c(
    name = "-log10(meta p)",
    option = "A"
  ) +

  scale_size(
    range = c(3,10),
    name = "Fraction improved"
  ) +

  labs(
    x = "-log10 Fisher meta‑p",
    y=""
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 45, hjust = 1),
    panel.spacing = unit(2, "lines"),
    strip.text = element_text(face = "bold")
  )
```

```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/tahoe_ctrl_paired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

## 10th Results {.tabset}
```{r}
tahoe_no_change_df <- readRDS(file.path(RESULTS_PATH, "tahoe/no_change_eval_table.rds"))
tahoe_ctrl_mean_df <- readRDS(file.path(RESULTS_PATH, "tahoe/ctrl_10th_mean_eval_table.rds"))
tahoe_ctrl_net_df <- readRDS(file.path(RESULTS_PATH, "tahoe/network_prop_10th_eval_table.rds"))
tahoe_ctrl_proj_e_df <- readRDS(file.path(RESULTS_PATH, "tahoe/10th_projectcor_eigen_eval_table.rds"))
tahoe_ctrl_proj_g_df <- readRDS(file.path(RESULTS_PATH, "tahoe/10th_projectcor_gsva_eval_table.rds"))
```

```{r}
plot_df <- bind_rows(
  tahoe_no_change_df %>%
    transmute(drug = gene, method = "No change", jacc, NES),

  tahoe_ctrl_mean_df %>%
    transmute(drug = gene, method = "Mean", jacc = jacc_TRUE, NES = NES_TRUE),

  tahoe_ctrl_net_df %>%
    transmute(drug = gene, method = "NetworkProp", jacc = jacc_TRUE, NES = NES_TRUE),

  tahoe_ctrl_proj_e_df %>%
    transmute(drug = gene, method = "ProjectCor (Eigen)", jacc = jacc_TRUE, NES = NES_TRUE),

  tahoe_ctrl_proj_g_df %>%
    transmute(drug = gene, method = "ProjectCor (GSVA)", jacc = jacc_TRUE, NES = NES_TRUE)
)

plot_long <- plot_df %>%
  dplyr::rename(Jaccard = jacc) %>%
  dplyr::group_by(drug, method) %>%
  dplyr::summarise_all(mean) %>% 
  dplyr::ungroup() %>%
  pivot_longer(c(Jaccard, NES), names_to = "metric", values_to = "value")

saveRDS(plot_long, file.path(RESULTS_PATH, "tahoe/ctrl_10th_unpaired_plot_df.rds"))

summary_df <- bind_rows(
  summarize_method(tahoe_ctrl_mean_df, "Mean"),
  summarize_method(tahoe_ctrl_net_df, "NetworkProp"),
  summarize_method(tahoe_ctrl_proj_e_df, "ProjectCor (Eigen)"),
  summarize_method(tahoe_ctrl_proj_g_df, "ProjectCor (GSVA)")
)
saveRDS(summary_df, file.path(RESULTS_PATH, "tahoe/ctrl_10th_paired_plot_df.rds"))
```

### Unpaired
```{r}
baseline_means <- plot_long %>%
  filter(method == "No change") %>%
  group_by(metric) %>%
  summarise(yintercept = median(value), .groups = "drop")

ggplot(plot_long, aes(method, value, fill = method)) +

  geom_violin(
    width = 0.9,
    alpha = 0.35,
    color = NA
  ) +

  ggbeeswarm::geom_beeswarm(
    size = 0.1,
    alpha = 0.7,
    cex = 0.7
  ) +

  stat_summary(
    fun = median,
    geom = "point",
    shape = 21,
    size = 3,
    fill = "white"
  ) +

  geom_hline(
    data = baseline_means,
    aes(yintercept = yintercept),
    linetype = "dotted",
    color = "grey50",
    linewidth = 0.8
  ) +

  facet_wrap(
    ~metric,
    scales = "free_y"
  ) +

  scale_fill_brewer(palette = "Pastel1") +
  scale_x_discrete(limits = c("No change", "Mean", "NetworkProp", "ProjectCor (Eigen)", "ProjectCor (GSVA)")) + 
  labs(
    x = "",
    y = "Score"
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 35, hjust = 1),
    legend.position = "none",
    strip.text = element_text(face = "bold"),
    plot.title = element_text(face = "bold")
  )
```
```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/tahoe_10th_unpaired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

### Paired
```{r}
ggplot(summary_df,
       aes(x = -log10(fisher_p),
           y = method,
           size = prop,
           color = -log10(fisher_p))) +

  geom_point(alpha = 0.9) +

  facet_wrap(~metric) +
  coord_cartesian(xlim = c(-0.6, 4.5)) +
  scale_color_viridis_c(
    name = "-log10(meta p)",
    option = "A"
  ) +

  scale_size(
    range = c(3,10),
    name = "Fraction Improved"
  ) +

  labs(
    x = "-log10 Fisher meta‑p",
    y=""
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 45, hjust = 1),
    panel.spacing = unit(2, "lines"),
    strip.text = element_text(face = "bold")
  )
```

```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/tahoe_10th_paired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

## 90th Results {.tabset}
```{r}
tahoe_no_change_df <- readRDS(file.path(RESULTS_PATH, "tahoe/no_change_eval_table.rds"))
tahoe_ctrl_mean_df <- readRDS(file.path(RESULTS_PATH, "tahoe/ctrl_90th_mean_eval_table.rds"))
tahoe_ctrl_net_df <- readRDS(file.path(RESULTS_PATH, "tahoe/network_prop_90th_eval_table.rds"))
tahoe_ctrl_proj_e_df <- readRDS(file.path(RESULTS_PATH, "tahoe/90th_projectcor_eigen_eval_table.rds"))
tahoe_ctrl_proj_g_df <- readRDS(file.path(RESULTS_PATH, "tahoe/90th_projectcor_gsva_eval_table.rds"))
```

```{r}
plot_df <- bind_rows(
  tahoe_no_change_df %>%
    transmute(drug = gene, method = "No change", jacc, NES),

  tahoe_ctrl_mean_df %>%
    transmute(drug = gene, method = "Mean", jacc = jacc_TRUE, NES = NES_TRUE),

  tahoe_ctrl_net_df %>%
    transmute(drug = gene, method = "NetworkProp", jacc = jacc_TRUE, NES = NES_TRUE),

  tahoe_ctrl_proj_e_df %>%
    transmute(drug = gene, method = "ProjectCor (Eigen)", jacc = jacc_TRUE, NES = NES_TRUE),

  tahoe_ctrl_proj_g_df %>%
    transmute(drug = gene, method = "ProjectCor (GSVA)", jacc = jacc_TRUE, NES = NES_TRUE)
)

plot_long <- plot_df %>%
  dplyr::rename(Jaccard = jacc) %>%
  dplyr::group_by(drug, method) %>%
  dplyr::summarise_all(mean) %>% 
  dplyr::ungroup() %>%
  pivot_longer(c(Jaccard, NES), names_to = "metric", values_to = "value")

saveRDS(plot_long, file.path(RESULTS_PATH, "tahoe/ctrl_90th_unpaired_plot_df.rds"))

summary_df <- bind_rows(
  summarize_method(tahoe_ctrl_mean_df, "Mean"),
  summarize_method(tahoe_ctrl_net_df, "NetworkProp"),
  summarize_method(tahoe_ctrl_proj_e_df, "ProjectCor (Eigen)"),
  summarize_method(tahoe_ctrl_proj_g_df, "ProjectCor (GSVA)")
)
saveRDS(summary_df, file.path(RESULTS_PATH, "tahoe/ctrl_90th_paired_plot_df.rds"))
```

### Unpaired
```{r}
baseline_means <- plot_long %>%
  filter(method == "No change") %>%
  group_by(metric) %>%
  summarise(yintercept = median(value), .groups = "drop")

ggplot(plot_long, aes(method, value, fill = method)) +

  geom_violin(
    width = 0.9,
    alpha = 0.35,
    color = NA
  ) +

  ggbeeswarm::geom_beeswarm(
    size = 0.1,
    alpha = 0.7,
    cex = 0.7
  ) +

  stat_summary(
    fun = median,
    geom = "point",
    shape = 21,
    size = 3,
    fill = "white"
  ) +

  geom_hline(
    data = baseline_means,
    aes(yintercept = yintercept),
    linetype = "dotted",
    color = "grey50",
    linewidth = 0.8
  ) +

  facet_wrap(
    ~metric,
    scales = "free_y"
  ) +

  scale_fill_brewer(palette = "Pastel1") +
  scale_x_discrete(limits = c("No change", "Mean", "NetworkProp", "ProjectCor (Eigen)", "ProjectCor (GSVA)")) + 
  labs(
    x = "",
    y = "Score"
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 35, hjust = 1),
    legend.position = "none",
    strip.text = element_text(face = "bold"),
    plot.title = element_text(face = "bold")
  )
```
```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/tahoe_90th_unpaired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

### Paired
```{r}
ggplot(summary_df,
       aes(x = -log10(fisher_p),
           y = method,
           size = prop,
           color = -log10(fisher_p))) +

  geom_point(alpha = 0.9) +

  facet_wrap(~metric) +
  coord_cartesian(xlim = c(-0.2, 7.5)) +
  scale_color_viridis_c(
    name = "-log10(meta p)",
    option = "A"
  ) +

  scale_size(
    range = c(3,10),
    name = "Fraction Improved"
  ) +

  labs(
    x = "-log10 Fisher meta‑p",
    y=""
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 45, hjust = 1),
    panel.spacing = unit(2, "lines"),
    strip.text = element_text(face = "bold")
  )
```


```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/tahoe_90th_paired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

## Trade
```{r}
trade_metrics_10th <- readRDS(file.path(RESULTS_PATH, "tahoe/trade_metric_correlations/projectCor/10th_projectcor_eigen_trade_joined.rds"))
trade_metrics_10th
```
```{r}
ggplot(trade_metrics_10th, aes(x = TI, y = delta_NES)) +
    geom_hline(yintercept = 0, color = "grey75", linewidth = 0.35) +
    geom_point(aes(color = gene), alpha = 0.72, size = 1.8) +
    geom_smooth(method = "lm", se = FALSE, color = "black", linewidth = 0.45) +
    theme_bw(base_size = 11) +
    theme(
      panel.grid.minor = element_blank(),
      legend.position = "right"
    ) +
    labs(
      x = "TI",
      y = "delta_NES",
      title = "TI vs delta NES (colored by perturbation)",
      color = "perturbation"
    )
```

```{r}
ggplot(trade_metrics_10th, aes(x = TI, y = delta_NES)) +
    geom_hline(yintercept = 0, color = "grey75", linewidth = 0.35) +
    geom_point(aes(color = target), alpha = 0.72, size = 1.8) +
    geom_smooth(method = "lm", se = FALSE, color = "black", linewidth = 0.45) +
    theme_bw(base_size = 11) +
    theme(
      panel.grid.minor = element_blank(),
      legend.position = "right"
    ) +
    labs(
      x = "TI",
      y = "delta_NES",
      title = "TI vs delta NES (colored by cell line)",
      color = "cellline"
    )
```


# Drugmatrix {.tabset}

## Control {.tabset}
```{r}
drugmatrix_no_change_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/no_change_eval_table.rds"))
drugmatrix_ctrl_mean_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/ctrl_mean_eval_table.rds"))
drugmatrix_ctrl_net_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/ctrl_networkprop_eval_table.rds"))
drugmatrix_ctrl_proj_e_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/ctrl_projectcor_eigen_eval_table.rds"))
drugmatrix_ctrl_proj_g_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/ctrl_projectcor_gsva_eval_table.rds"))
```

### Unpaired

```{r}
plot_df <- bind_rows(
  drugmatrix_no_change_df %>%
    transmute(drug = gene, method = "No change", jacc, NES),

  drugmatrix_ctrl_mean_df %>%
    transmute(drug = gene, method = "Mean", jacc = jacc_TRUE, NES = NES_TRUE),

  drugmatrix_ctrl_net_df %>%
    transmute(drug = gene, method = "NetworkProp", jacc = jacc_TRUE, NES = NES_TRUE),

  drugmatrix_ctrl_proj_e_df %>%
    transmute(drug = gene, method = "ProjectCor (Eigen)", jacc = jacc_TRUE, NES = NES_TRUE),

  drugmatrix_ctrl_proj_g_df %>%
    transmute(drug = gene, method = "ProjectCor (GSVA)", jacc = jacc_TRUE, NES = NES_TRUE)
)

plot_long <- plot_df %>%
  dplyr::rename(Jaccard = jacc) %>%
  pivot_longer(c(Jaccard, NES), names_to = "metric", values_to = "value")

saveRDS(plot_long, file.path(RESULTS_PATH, "drugmatrix/ctrl_unpaired_plot_df.rds"))

summary_df <- bind_rows(
  summarize_method(drugmatrix_ctrl_mean_df, "Mean"),
  summarize_method(drugmatrix_ctrl_net_df, "NetworkProp"),
  summarize_method(drugmatrix_ctrl_proj_e_df, "ProjectCor (Eigen)"),
  summarize_method(drugmatrix_ctrl_proj_g_df, "ProjectCor (GSVA)")
)
saveRDS(summary_df, file.path(RESULTS_PATH, "drugmatrix/ctrl_paired_plot_df.rds"))
```
```{r}
baseline_means <- plot_long %>%
  filter(method == "No change") %>%
  group_by(metric) %>%
  summarise(yintercept = median(value), .groups = "drop")

ggplot(plot_long, aes(method, value, fill = method)) +

  geom_violin(
    width = 0.9,
    alpha = 0.35,
    color = NA
  ) +

  ggbeeswarm::geom_beeswarm(
    size = 0.1,
    alpha = 0.7,
    cex = 0.7
  ) +

  stat_summary(
    fun = median,
    geom = "point",
    shape = 21,
    size = 3,
    fill = "white"
  ) +

  geom_hline(
    data = baseline_means,
    aes(yintercept = yintercept),
    linetype = "dotted",
    color = "grey50",
    linewidth = 0.8
  ) +

  facet_wrap(
    ~metric,
    scales = "free_y"
  ) +

  scale_fill_brewer(palette = "Pastel1") +
  scale_x_discrete(limits = c("No change", "Mean", "NetworkProp", "ProjectCor (Eigen)", "ProjectCor (GSVA)")) + 
  labs(
    x = "",
    y = "Score"
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 35, hjust = 1),
    legend.position = "none",
    strip.text = element_text(face = "bold"),
    plot.title = element_text(face = "bold")
  )
```

```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/drugmatrix_ctrl_unpaired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

### Paired

#### Swarm
```{r}
plot_df <- bind_rows(
  drugmatrix_ctrl_mean_df %>%
    transmute(drug = gene, method = "Mean", jacc_delta = jacc_TRUE - jacc_FALSE, NES_delta = NES_TRUE - NES_FALSE),

  drugmatrix_ctrl_net_df %>%
    transmute(drug = gene, method = "NetworkProp", jacc_delta = jacc_TRUE - jacc_FALSE, NES_delta = NES_TRUE - NES_FALSE),

  drugmatrix_ctrl_proj_e_df %>%
    transmute(drug = gene, method = "ProjectCor (Eigen)", jacc_delta = jacc_TRUE - jacc_FALSE, NES_delta = NES_TRUE - NES_FALSE),

  drugmatrix_ctrl_proj_g_df %>%
    transmute(drug = gene, method = "ProjectCor (GSVA)", jacc_delta = jacc_TRUE - jacc_FALSE, NES_delta = NES_TRUE - NES_FALSE)
)

plot_long <- plot_df %>%
  rename(`Jaccard ∆` = jacc_delta, `NES ∆` = NES_delta) %>%
  pivot_longer(c(`Jaccard ∆`, `NES ∆`), names_to = "metric", values_to = "value")

saveRDS(plot_long, file.path(RESULTS_PATH, "drugmatrix/ctrl_paired_swarm_plot_df.rds"))
```


```{r}
ggplot(plot_long, aes(method, value, fill = method)) +

  geom_violin(
    width = 0.9,
    alpha = 0.35,
    color = NA
  ) +

  ggbeeswarm::geom_beeswarm(
    size = 0.1,
    alpha = 0.7,
    cex = 0.7
  ) +

  stat_summary(
    fun = median,
    geom = "point",
    shape = 21,
    size = 3,
    fill = "white"
  ) +

  geom_hline(
    yintercept = 0,
    linetype = "dotted",
    color = "grey50",
    linewidth = 0.8
  ) +

  facet_wrap(
    ~metric,
    scales = "free_y"
  ) +

  scale_fill_brewer(palette = "Pastel1") +
  scale_x_discrete(limits = c("Mean", "NetworkProp", "ProjectCor (Eigen)", "ProjectCor (GSVA)")) + 

  labs(
    x = "",
    y = "Score"
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 35, hjust = 1),
    legend.position = "none",
    strip.text = element_text(face = "bold"),
    plot.title = element_text(face = "bold")
  )
```

```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/drugmatrix_ctrl_paired_swarm.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

#### Dot Plot
```{r}
ggplot(summary_df,
       aes(x = -log10(fisher_p),
           y = method,
           size = prop,
           color = -log10(fisher_p))) +

  geom_point(alpha = 0.9) +

  facet_wrap(~metric) +
  coord_cartesian(xlim = c(-0.02, 0.125)) +
  scale_color_viridis_c(
    name = "-log10(meta p)",
    option = "A"
  ) +

  scale_size(
    range = c(3,10),
    name = "Fraction improved"
  ) +

  labs(
    x = "-log10 Fisher meta‑p",
    y=""
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 45, hjust = 1),
    panel.spacing = unit(2, "lines"),
    strip.text = element_text(face = "bold")
  )
```

```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/drugmatrix_ctrl_paired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

## 10th Results {.tabset}
```{r}
drugmatrix_no_change_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/no_change_eval_table.rds"))
drugmatrix_ctrl_mean_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/ctrl_10th_mean_eval_table.rds"))
drugmatrix_ctrl_net_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/ctrl_10th_networkprop_eval_table.rds"))
drugmatrix_ctrl_proj_e_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/10th_projectcor_eigen_eval_table.rds"))
drugmatrix_ctrl_proj_g_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/10th_projectcor_gsva_eval_table.rds"))
```

```{r}
plot_df <- bind_rows(
  drugmatrix_no_change_df %>%
    transmute(drug = gene, method = "No change", jacc, NES),

  drugmatrix_ctrl_mean_df %>%
    transmute(drug = gene, method = "Mean", jacc = jacc_TRUE, NES = NES_TRUE),

  drugmatrix_ctrl_net_df %>%
    transmute(drug = gene, method = "NetworkProp", jacc = jacc_TRUE, NES = NES_TRUE),

  drugmatrix_ctrl_proj_e_df %>%
    transmute(drug = gene, method = "ProjectCor (Eigen)", jacc = jacc_TRUE, NES = NES_TRUE),

  drugmatrix_ctrl_proj_g_df %>%
    transmute(drug = gene, method = "ProjectCor (GSVA)", jacc = jacc_TRUE, NES = NES_TRUE)
)

plot_long <- plot_df %>%
  dplyr::rename(Jaccard = jacc) %>%
  dplyr::group_by(drug, method) %>%
  dplyr::summarise_all(mean) %>% 
  dplyr::ungroup() %>%
  pivot_longer(c(Jaccard, NES), names_to = "metric", values_to = "value")

saveRDS(plot_long, file.path(RESULTS_PATH, "drugmatrix/ctrl_10th_unpaired_plot_df.rds"))

summary_df <- bind_rows(
  summarize_method(drugmatrix_ctrl_mean_df, "Mean"),
  summarize_method(drugmatrix_ctrl_net_df, "NetworkProp"),
  summarize_method(drugmatrix_ctrl_proj_e_df, "ProjectCor (Eigen)"),
  summarize_method(drugmatrix_ctrl_proj_g_df, "ProjectCor (GSVA)")
)
saveRDS(summary_df, file.path(RESULTS_PATH, "drugmatrix/ctrl_10th_paired_plot_df.rds"))
```

### Unpaired
```{r}
baseline_means <- plot_long %>%
  filter(method == "No change") %>%
  group_by(metric) %>%
  summarise(yintercept = median(value), .groups = "drop")

ggplot(plot_long, aes(method, value, fill = method)) +

  geom_violin(
    width = 0.9,
    alpha = 0.35,
    color = NA
  ) +

  ggbeeswarm::geom_beeswarm(
    size = 0.1,
    alpha = 0.7,
    cex = 0.7
  ) +

  stat_summary(
    fun = median,
    geom = "point",
    shape = 21,
    size = 3,
    fill = "white"
  ) +

  geom_hline(
    data = baseline_means,
    aes(yintercept = yintercept),
    linetype = "dotted",
    color = "grey50",
    linewidth = 0.8
  ) +

  facet_wrap(
    ~metric,
    scales = "free_y"
  ) +

  scale_fill_brewer(palette = "Pastel1") +
  scale_x_discrete(limits = c("No change", "Mean", "NetworkProp", "ProjectCor (Eigen)", "ProjectCor (GSVA)")) + 
  labs(
    x = "",
    y = "Score"
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 35, hjust = 1),
    legend.position = "none",
    strip.text = element_text(face = "bold"),
    plot.title = element_text(face = "bold")
  )
```
```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/drugmatrix_10th_unpaired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

### Paired
```{r}
ggplot(summary_df,
       aes(x = -log10(fisher_p),
           y = method,
           size = prop,
           color = -log10(fisher_p))) +

  geom_point(alpha = 0.9) +

  facet_wrap(~metric) +
  coord_cartesian(xlim = c(-0.6, 4.5)) +
  scale_color_viridis_c(
    name = "-log10(meta p)",
    option = "A"
  ) +

  scale_size(
    range = c(3,10),
    name = "Fraction Improved"
  ) +

  labs(
    x = "-log10 Fisher meta‑p",
    y=""
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 45, hjust = 1),
    panel.spacing = unit(2, "lines"),
    strip.text = element_text(face = "bold")
  )
```

```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/drugmatrix_10th_paired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

## 90th Results {.tabset}
```{r}
drugmatrix_no_change_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/no_change_eval_table.rds"))
drugmatrix_ctrl_mean_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/ctrl_90th_mean_eval_table.rds"))
drugmatrix_ctrl_net_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/ctrl_90th_networkprop_eval_table.rds"))
drugmatrix_ctrl_proj_e_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/90th_projectcor_eigen_eval_table.rds"))
drugmatrix_ctrl_proj_g_df <- readRDS(file.path(RESULTS_PATH, "drugmatrix/90th_projectcor_gsva_eval_table.rds"))
```

```{r}
plot_df <- bind_rows(
  drugmatrix_no_change_df %>%
    transmute(drug = gene, method = "No change", jacc, NES),

  drugmatrix_ctrl_mean_df %>%
    transmute(drug = gene, method = "Mean", jacc = jacc_TRUE, NES = NES_TRUE),

  drugmatrix_ctrl_net_df %>%
    transmute(drug = gene, method = "NetworkProp", jacc = jacc_TRUE, NES = NES_TRUE),

  drugmatrix_ctrl_proj_e_df %>%
    transmute(drug = gene, method = "ProjectCor (Eigen)", jacc = jacc_TRUE, NES = NES_TRUE),

  drugmatrix_ctrl_proj_g_df %>%
    transmute(drug = gene, method = "ProjectCor (GSVA)", jacc = jacc_TRUE, NES = NES_TRUE)
)

plot_long <- plot_df %>%
  dplyr::rename(Jaccard = jacc) %>%
  dplyr::group_by(drug, method) %>%
  dplyr::summarise_all(mean) %>% 
  dplyr::ungroup() %>%
  pivot_longer(c(Jaccard, NES), names_to = "metric", values_to = "value")

saveRDS(plot_long, file.path(RESULTS_PATH, "drugmatrix/ctrl_90th_unpaired_plot_df.rds"))

summary_df <- bind_rows(
  summarize_method(drugmatrix_ctrl_mean_df, "Mean"),
  summarize_method(drugmatrix_ctrl_net_df, "NetworkProp"),
  summarize_method(drugmatrix_ctrl_proj_e_df, "ProjectCor (Eigen)"),
  summarize_method(drugmatrix_ctrl_proj_g_df, "ProjectCor (GSVA)")
)
saveRDS(summary_df, file.path(RESULTS_PATH, "drugmatrix/ctrl_90th_paired_plot_df.rds"))
```

### Unpaired
```{r}
baseline_means <- plot_long %>%
  filter(method == "No change") %>%
  group_by(metric) %>%
  summarise(yintercept = median(value), .groups = "drop")

ggplot(plot_long, aes(method, value, fill = method)) +

  geom_violin(
    width = 0.9,
    alpha = 0.35,
    color = NA
  ) +

  ggbeeswarm::geom_beeswarm(
    size = 0.1,
    alpha = 0.7,
    cex = 0.7
  ) +

  stat_summary(
    fun = median,
    geom = "point",
    shape = 21,
    size = 3,
    fill = "white"
  ) +

  geom_hline(
    data = baseline_means,
    aes(yintercept = yintercept),
    linetype = "dotted",
    color = "grey50",
    linewidth = 0.8
  ) +

  facet_wrap(
    ~metric,
    scales = "free_y"
  ) +

  scale_fill_brewer(palette = "Pastel1") +
  scale_x_discrete(limits = c("No change", "Mean", "NetworkProp", "ProjectCor (Eigen)", "ProjectCor (GSVA)")) + 
  labs(
    x = "",
    y = "Score"
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 35, hjust = 1),
    legend.position = "none",
    strip.text = element_text(face = "bold"),
    plot.title = element_text(face = "bold")
  )
```
```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/drugmatrix_90th_unpaired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```

### Paired
```{r}
ggplot(summary_df,
       aes(x = -log10(fisher_p),
           y = method,
           size = prop,
           color = -log10(fisher_p))) +

  geom_point(alpha = 0.9) +

  facet_wrap(~metric) +
  coord_cartesian(xlim = c(-0.2, 7.5)) +
  scale_color_viridis_c(
    name = "-log10(meta p)",
    option = "A"
  ) +

  scale_size(
    range = c(3,10),
    name = "Fraction Improved"
  ) +

  labs(
    x = "-log10 Fisher meta‑p",
    y=""
  ) +

  theme_classic(base_size = 15) +

  theme(
    axis.text.x = element_text(angle = 45, hjust = 1),
    panel.spacing = unit(2, "lines"),
    strip.text = element_text(face = "bold")
  )
```


```{r}
ggsave(file.path(PROJECT_PATH, "results/plots/drugmatrix_90th_paired.png"),
  width = 8,
  height = 5,
  dpi = 400
)
```
